import numpy as np
def gerenciador_microrrede():
#    DATABASE_URL, engine, SessionLocal, Base = Configure()
 #   session = SessionLocal()

  #  microrredes = session.query(Microrrede).all()
   # for microrrede in microrredes:
   #     print(microrrede.nome)
    compra =  np.zeros(1440)
    venda = np.zeros(1440)

   # demanda = microrrede.carga.demanda  # Demanda da microrrede ao 

    return compra, venda

# Compara as otmizações de diferentes métodos. 

# Apenas uso da rede elétrica convencional

# Apenas o uso de gerador solar/Bess 

# Uso combinado de gerador solar/Bess e rede elétrica convencional
